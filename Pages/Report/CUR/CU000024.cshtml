@page
@model GenericReportPageModel
@{
    var cfg = new ReportDirectConfig {
        Title = "CU000024 使用者密碼查詢",
        SpName = "CURd使用者密碼查詢",   // SP 名稱（不要帶 @）
        ReportName = "CURd使用者密碼查詢",         // rpt 檔名（不含 .rpt）
        Params = new() {
            new ParamSpec { Name="UserId", Label="使用者代碼", Ui="select", LookupKey="user" }
        }
    };
}
@await Html.PartialAsync("~/Pages/Shared/_ReportDirectPreview.cshtml", cfg)